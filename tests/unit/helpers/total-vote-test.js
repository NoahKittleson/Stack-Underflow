import { totalVote } from 'stack-underflow/helpers/total-vote';
import { module, test } from 'qunit';

module('Unit | Helper | total vote');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = totalVote([42]);
  assert.ok(result);
});
